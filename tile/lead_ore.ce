from core.template.item import Tile, ScrapTier
from siege.graphic import Color

leadOre = Tile(
    name = "LeadOre",
    durability = 340,
    priority = 200,
    level = 8,
    groups = ['ore', 'natural'],
    sounds = 'stone',
    particleColor = Color(17, 88, 112),
    mapGroundColor = Color(17, 88, 112),
    mapWallColor = Color(15, 76, 97)
)

leadOre.isItem(
    classification = "Mineral",
    stack = 99,
    scraps = ScrapTier.Two
)
